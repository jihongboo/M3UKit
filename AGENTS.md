# AGENTS

## Code Style Rules

- Place all `private` methods inside `extension` blocks instead of declaring them in the main type body.
- For parser types, keep public API in the primary type declaration and helper logic in `private extension`.
